define(['uwr/jquery', 'uwr/initState'], function ($, initState) {

    /**
     * Instances of the Observer class can be observed by other objects. The observed object can raise its own
     * events and observer objects can attach event handlers to the observed object. Triggering an event by
     * the observed object means the execution of all event handlers attached to that particular event.
     *
     * Event names are strings. Event handlers are functions with one argument, the data sent by the observed object.
     * Within event handlers the 'this' is mapped to the observed object.
     */
    function Observable() {
        var handlers = {};

        this.on = function (name, func) {
            if (!handlers[name]) {
                handlers[name] = [];
            }
            handlers[name].push(func);
        };

        this.trigger = function (name, data) {
            if (handlers[name] && handlers[name].length > 0) {
                for (var i = 0, l = handlers[name].length; i < l; i++) {
                    handlers[name][i].call(null, data);
                }
            }
        };

        this.off = function (name, func) {
            var l = arguments.length;
            if (l === 0) {
                handlers = {};
            } else if (l === 1 && handlers[name]) {
                delete handlers[name];
            } else if (l === 2 && handlers[name] && handlers[name].length > 0) {
                for (var i = 0, k = handlers[name].length; i < k; i++) {
                    if (handlers[name][i] === func) {
                        delete handlers[name][i];
                        return;
                    }
                }
            }
        };
    }

    /**
     * A class which is a wrapper of a single value. It extends the Observable class, so it throws a 'change:value'
     * event on value changed.
     *
     * @param val The initial value of this class.
     * @param changeCallback The callback method which is invoked on value change. This can define additional effect
     * associated to the change event.
     */
    function ObservableValue(val, changeCallback) {
        var self = this;

        this.value = function () {
            if (arguments.length) {
                var newVal = arguments[0];
                if (newVal !== val) {
                    val = newVal;
                    self.trigger('change:value', val);
                    if (typeof changeCallback === 'function') {
                        invoke(changeCallback, val);
                    }
                }
            } else {
                return val;
            }
        };

        Observable.call(this);
    }

    function callWidgetTypeInit(selector, namespace, callback) {
        if ($(selector).length > 0) {
            namespace.init(callback);
        } else {
            callback();
        }
    }

    /**
     * Returns the object associated to the given fully qualified name in the window object, e.g. 'document.cookie'.
     * If not found, unassigned will return.
     */
    function getObjectByName(name) {
        var i,
            obj = window,
            parts = name.split('.'),
            len = parts.length;
        for (i = 0; i < len; i++) {
            obj = obj[parts[i]];
            if (obj === undefined) {
                break;
            }
        }
        return obj;
    }

    /**
     * This function is to be used to register a given widget type: it adds the widget type to the
     * global initialization state, but only if the widget has an init() function defined.
     *
     * @param cssClass The main CSS class associated with the widget.
     * @param namespace The widget type's API functions' namespace string.
     */
    function registerWidgetType(cssClass, namespace) {
        var namespaceObj = getObjectByName(namespace);
        if (typeof namespaceObj.init === 'function') {
            initState.addInitializableWidgetType({
                selector: '.' + cssClass,
                namespace: namespaceObj
            });
        }
    }

    /**
     * Initialize all widget types, already registered by registerWidgetType.
     *
     * @param callback A function which is called after all widget types have been initialized.
     */
    function initAllWidgetTypes(callback) {
        $(function () {
            var widgetTypes = initState.getInitializableWidgetTypes(),
                count = widgetTypes.length,
                i;

            initState.initialized = false;

            if (typeof callback === 'function') {
                initState.addReadyHandler(callback);
            }

            for (i = 0; i < widgetTypes.length; i++) {
                /* jshint loopfunc:true */
                callWidgetTypeInit(widgetTypes[i].selector, widgetTypes[i].namespace, function () {
                    count--;
                    if (count === 0) {
                        initState.ready();
                    }
                });
                /* jshint loopfunc:false */
            }
        });
    }

    /**
     * Invokes a function passed in as the first argument. If there are more arguments passed in, they will be used
     * as parameters for the function being invoked.
     * If the first parameter is not a function, then nothing will be done.
     *
     * @param First param is the function to invoke, any further params will be passed in to that function.
     *     If the first parameter is not a function, then nothing will be done.
     */
    function invoke(fn) {
        var args = [],
            i;

        if (typeof fn === 'function') {
            if (arguments.length > 1) {
                for (i = 1; i < arguments.length; i++) {
                    args.push(arguments[i]);
                }
            }
            fn.apply(null, args);
        }
    }

    function buildClass() {
        /* jshint ignore:start */
        /*
         * Simple JavaScript Inheritance
         * By John Resig http://ejohn.org/
         * http://ejohn.org/blog/simple-javascript-inheritance/
         * MIT Licensed.
         *
         * For use in UW4 the constructor method's name became _init() instead of the original init().
         */

        var initializing = false,
        fnTest = /xyz/.test(function(){xyz;}) ? /\b_super\b/ : /.*/,

        // The base Class implementation (does nothing)
        Class = function() {};

        // Create a new Class that inherits from this class
        Class.extend = function(prop) {
            var _super = this.prototype;

            // Instantiate a base class (but only create the instance,
            // don't run the init constructor)
            initializing = true;
            var prototype = new this();
            initializing = false;

            // Copy the properties over onto the new prototype
            for (var name in prop) {
                // Check if we're overwriting an existing function
                prototype[name] = typeof prop[name] == 'function' &&
                    typeof _super[name] == 'function' && fnTest.test(prop[name])
                        ? (function(name, fn) {
                            return function() {
                                var tmp = this._super;

                                // Add a new ._super() method that is the same method
                                // but on the super-class
                                this._super = _super[name];

                                // The method only need to be bound temporarily, so we
                                // remove it when we're done executing
                                var ret = fn.apply(this, arguments);
                                this._super = tmp;

                                return ret;
                            };
                        })(name, prop[name])
                        : prop[name];
            }

            // The dummy class constructor
            function Class() {
                // All construction is actually done in the init method
                if ( !initializing && this._init) {
                    this._init.apply(this, arguments);
                }
            }

            // Populate our constructed prototype object
            Class.prototype = prototype;

            // Enforce the constructor to be what we expect
            Class.prototype.constructor = Class;

            // And make this class extendable
            Class.extend = arguments.callee;

            return Class;
        };

        return Class;
        /* jshint ignore:end */
    }

    function isString(value) {
        return $.type(value) === 'string';
    }

    function stringToObject(str) {
        if (isString(str)) {
            str = JSON.parse(str);
        }
        return str;
    }

    function objectToString(obj) {
        if (isString(obj)) {
            return obj;
        }
        if ($.isPlainObject(obj)) {
            return JSON.stringify(obj);
        }
        return '' + obj;
    }

    function isTouchExplorer() {
        return (navigator.userAgent.indexOf('MSIE') > -1 || navigator.userAgent.indexOf('rv:11.0') > -1)
                && navigator.userAgent.indexOf('Touch') > -1;
    }

    /**
     * This function checks if we are on a touch device (usually a tablet or a smart phone)
     */
    function isTouchDevice() {
        return isTouchExplorer() ? true : !!('ontouchstart' in window);
    }

    function isIe7() {
        return isIeVersion(7);
    }

    function isIe8() {
        return isIeVersion(8);
    }

    function isIe9() {
        return isIeVersion(9);
    }

    function isIeVersion(version) {
        return navigator.userAgent.indexOf('MSIE ' + version) > -1;
    }

    /**
     * @param {string} A JS reference string.
     * @return The reference string cleaned up. No javascript calls, functions and statements are allowed
     *         in this reference - not allowed characters are removed.
     */
    function sanitizeReferenceString(reference) {
        return reference.replace(/[^\w\.\[\]\'\"-]/g, '');
    }

    /**
     * @param {string} A JS expression string.
     * @return The reference of JS expression string.
     */
    function resolveReference(str) {
        var reference;
        if (str) {
            reference = eval(common.sanitizeReferenceString(str)); // jshint ignore:line
        }
        return reference;
    }

    /**
     * The missing signum function of Math. Value should be a number.
     */
    function sign(value) {
        return value > 0 ? 1 : value < 0 ? -1 : 0;
    }

    /**
     * Get data attribute of the elem and format it.
     *
     * @param $elem The jQuery elem which data is retrieved.
     * @param name The name of the data attribute.
     * @param defaultValue The default value if no data attribute found.
     * @param formatter Optional, to format value.
     * @return The value of the data attribute or the defaultValue if no data attribute found.
     */
    function data($elem, name, defaultValue, formatter) {
        var elemData = $elem.data(name),
            value = elemData !== undefined ? elemData : defaultValue;
        return formatter ? formatter(value) : value;
    }

    /**
     * Get data attribute of the elem as integer.
     *
     * @param $elem The jQuery elem which data is retrieved.
     * @param name The name of the data attribute.
     * @param defaultValue The default value if no data attribute found.
     * @return The integer value of the data attribute or the defaultValue if no data attribute found.
     */
    function intData($elem, name, defaultValue) {
        return data($elem, name, defaultValue, parseInt);
    }

    /**
     * Get data attribute of the elem as JSON object.
     *
     * @param $elem The jQuery elem of which data is retrieved.
     * @param name The name of the data attribute.
     * @param defaultValue The default value if no data attribute found or the data is not valid JSON.
     *        It can be of any type.
     * @return The JSON object of the data attribute or the given defaultValue if no data attribute found
     *         or the data is not valid JSON.
     */
    function jsonData($elem, name, defaultValue) {
        return data($elem, name, defaultValue, function (val) {
            var json;
            try {
                json = stringToObject(val);
            } catch (e) {}
            if (!json) {
                json = defaultValue;
            }
            return json;
        });
    }

    /**
     * Get the configuration from the HTML element attribute.
     *
     * @param $elem The jQuery elem of which attribute content is retrived.
     * @param attributeName The name of the data attribute (without the data- prefix).
     * @param args The optional arguments array of the configuration provider function.
     * @return The JSON configuration object which is provided based on the attribute's value.
     */
    function resolveConfig($elem, attributeName, args) {
        var obj = jsonData($elem, attributeName, $elem.data(attributeName)),
            config = {},
            referencedObj;

        if (obj) {
            if ($.isPlainObject(obj)) {
                config = obj;
            } else {
                referencedObj = resolveReference(String(obj));
                if ($.isFunction(referencedObj)) {
                    config = referencedObj.apply(undefined, args || []);
                } else {
                    // Clone so that we don't modify the client's config object
                    config = $.extend(true, {}, referencedObj);
                }
            }
        }

        return config;
    }

    /**
     * @return The event object regardless of browser type
     */
    function fixEvent(event) {
        return event || window.event;
    }

    /**
     * Fire an event on a defined HTML element using the browser's infrastructure.
     * @param eventType The type of the event like change or click.
     * @param element The HTML element which is the target of the event.
     */
    function dispatchEvent(eventType, element) {
        var event;
        if (typeof Event === 'function') { //latest API
            event = new Event(eventType);
        } else if (document.createEvent) { //modern IE
            event = document.createEvent('Event');
            event.initEvent(eventType, false, false);
        } else { //IE fallback
            event = document.createEventObject();
            event.eventType = eventType;
        }

        event.eventName = eventType;

        if (element.dispatchEvent) {
            element.dispatchEvent(event);
        } else { // before IE9
            element.fireEvent('on' + eventType, event);
        }

    }

    /**
     * @return The event's target in jQuery object regardless of browser type
     * (IE8 doesn't pass the instance of the 'event' object to the handler,
     *  this must be taken from the global 'window' object)
     */
    function getEventTarget(event) {
        var ev = fixEvent(event);
        return $(ev.target || ev.srcElement);
    }

    var common = {
        /**
         * The Class object as described in the 'Simple JavaScript Inheritance' page.
         * Only difference is that the user defined class's constructor should be _init()
         * and not init() as the page suggests.
         */
        Class: buildClass(),
        Observable: Observable,
        ObservableValue: ObservableValue,
        registerWidgetType: registerWidgetType,
        dispatchEvent: dispatchEvent,
        initAllWidgetTypes: initAllWidgetTypes,
        invoke: invoke,
        isString: isString,
        isTouchDevice: isTouchDevice,
        isIe7: isIe7,
        isIe8: isIe8,
        isIe9: isIe9,
        sanitizeReferenceString: sanitizeReferenceString,
        resolveReference: resolveReference,
        resolveConfig: resolveConfig,
        sign: sign,
        stringToObject: stringToObject,
        objectToString: objectToString,
        data: data,
        intData: intData,
        jsonData: jsonData,
        fixEvent: fixEvent,
        getEventTarget: getEventTarget
    };

    return common;
});È=<gÕ      fyôgfyôgGçÐfyù   ¯    O^userContextId=3&partitionKey=%28https%2Cubs.com%29,:https://ebanking-ch2.ubs.com/login/resources/nevislogrend/applications/web_ch/webdata/ubswidgets/shared/modules/common.js strongly-framed 1 security-info  request-method GET response-head HTTP/1.1 200 OK
Date: Mon, 24 Jun 2024 22:34:09 GMT
Server: Apache
Content-Type: application/javascript
X-Frame-Options: sameorigin
X-XSS-Protection: 1; mode=block
Content-Length: 16197
Cache-Control: private,max-age=1200,s-maxage=1200
Content-Security-Policy: script-src 'self' 'unsafe-inline' 'unsafe-eval' blob:
X-Content-Type-Options: nosniff
Feature-Policy: camera 'self'; microphone 'self'
stidId: yIuAukhPLAIaNqOZ
trID: 1f0a8d18-0fe24b-d90a4c4a-1904c629bae-0085ed62
Expires: Tue, 01 Jan 2030 00:00:00 GMT
Strict-Transport-Security: max-age=31536000
 original-response-headers Date: Mon, 24 Jun 2024 22:34:09 GMT
Server: Apache
Content-Type: application/javascript
X-Frame-Options: sameorigin
X-XSS-Protection: 1; mode=block
Content-Length: 16197
Cache-Control: private,max-age=1200,s-maxage=1200
Content-Security-Policy: script-src 'self' 'unsafe-inline' 'unsafe-eval' blob:
X-Content-Type-Options: nosniff
Feature-Policy: camera 'self'; microphone 'self'
stidId: yIuAukhPLAIaNqOZ
trID: 1f0a8d18-0fe24b-d90a4c4a-1904c629bae-0085ed62
Expires: Tue, 01 Jan 2030 00:00:00 GMT
Strict-Transport-Security: max-age=31536000
Keep-Alive: timeout=5, max=6
Connection: Keep-Alive
 ctid 2 uncompressed-len 0 net-response-time-onstart 27 net-response-time-onstop 30   ?E