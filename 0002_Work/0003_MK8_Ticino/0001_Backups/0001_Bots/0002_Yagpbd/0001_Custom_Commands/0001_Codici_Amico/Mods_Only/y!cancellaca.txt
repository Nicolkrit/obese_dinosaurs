{{/* Verifica comando */}}
{{if eq (len .CmdArgs) 1}}
  {{/* Cerca l'id dell'utente menzionato */}}
  {{$user := userArg (index .CmdArgs 0)}}
  {{if $user}}
    {{/* Cancella il codice amico dal database */}}
    {{dbDel $user.ID "friendcode"}}
    Codice amico cancellato per {{$user.Username}}.
  {{else}}
    Utente non trovato.
  {{end}}
{{else}}
  Utilizzo: y!cancellaca @utente
{{end}}